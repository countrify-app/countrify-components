import { Meta, Typeset } from "@storybook/addon-docs/blocks";
import { font } from "../_utils/branding";

<Meta title="Styles/Fonts" />

# Font Scaling

**Text Font:** Karla 400(regular), Karla 700(medium)

<Typeset
  fontSizes={Object.keys(font).map((scale) => font[scale].size)}
  sampleText="The quick brown fox jumps over lazy dog"
  fontFamily="Karla"
/>

**Title Font:** YoungSerif

<Typeset
  fontSizes={Object.keys(font).map((scale) => font[scale].size)}
  sampleText="The quick brown fox jumps over lazy dog"
  fontFamily="YoungSerif"
/>
